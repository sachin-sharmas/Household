{
  "name": "household-grocery-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Household Grocery Management System - a shared grocery and food inventory dashboard built with React, Node, Express, and MongoDB.",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
